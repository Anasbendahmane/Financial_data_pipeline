version: 2

sources:
    - name: BTC_SCHEMA
      database: BTC
      schema: BTC_SCHEMA
      tables:
        - name: BTC_TABLE
          config:
            freshness:
              warn_after:
                count: 1
                period: hour
              error_after:
                count: 3
                period: hour

            loaded_at_field: BLOCK_TIMESTAMP 
